# CloudHorus - Core Dependencies
graphviz>=0.20
tqdm>=4.65.0
colorama>=0.4.6
Pillow>=9.0.0
python-hcl2>=8.1.0

# GUI — Modern Web UI (pywebview)
pywebview>=5.0
# On Linux, also install: PyQt5 PyQtWebEngine qtpy (handled by launcher.sh)

# Azure SDK — needed for Live Mode (scanning real subscriptions)
azure-identity>=1.15.0
azure-mgmt-resource>=23.0.0
azure-mgmt-network>=23.0.0
azure-mgmt-subscription>=3.0.0
azure-core>=1.28.0

# Optional: Draw.io export support
# graphviz2drawio — installed separately (pygraphviz has no Windows wheel)
# graphviz2drawio>=1.1.0